{
  "name": "ac-tasks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pregenerate": "./convert-to-csv",
    "generate": "node generate-data.js",
    "build": "npm run generate && elm make src/Main.elm --output=main.js"
  },
  "dependencies": {
    "cytoscape": "^3.34.0",
    "cytoscape-dagre": "^4.0.0"
  }
}
